import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { AddIcon } from '../Icons';

import { Asset } from './Asset';

<Meta
  title="Asset"
  component={Asset}
  decorators={[]}
  parameters={{ name: 'Name' }}
/>

# Asset

Used to display the various SVG icons found at `/Icons`.

<Canvas>
  <Story name="Asset">
    <Asset icon={AddIcon} description="Add Component" height={25} width={25} />
  </Story>
</Canvas>
